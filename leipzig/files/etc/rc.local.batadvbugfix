#!/bin/sh 
# Freifunk Leipzig cron
# $Id: freifunk-leipzig  2016-04-20 23:51:08Z ufo $

#batctl -m bat12 if add eth0.1
#batctl -m bat12 if add eth0.2
#batctl -m bat12 if add eth0
#batctl -m bat12 if add eth1

#batctl -m bat14 if add wlan0.14
#batctl -m bat12 if add wlan1.12
#batctl -m bat14 if add wlan1.14
#ip -6 rule del dev br-mesh12 unreachable prio 20001
#ip -6 rule del dev br-mesh14 unreachable prio 20001